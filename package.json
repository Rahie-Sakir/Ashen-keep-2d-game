{
  "name": "soulsfan-games",
  "version": "1.0.0",
  "private": true,
  "description": "SoulsFan Games browser action platformer with React, TypeScript, FastAPI, and JSON persistence",
  "scripts": {
    "install:all": "python -m pip install -r backend/requirements.txt && npm --prefix frontend install",
    "start:server": "python -m backend",
    "dev:server": "python -m backend --reload",
    "dev:client": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "test": "python -m pytest backend/tests"
  }
}
