# SoulsFan Games Frontend

React + Vite + TypeScript frontend for SoulsFan Games. The game runtime is an
HTML5 Canvas simulation in `src/game`, with React screens for menus, codex,
play, and leaderboard views.

## Scripts

```bash
npm run dev
npm run build
npm run preview
```

The root project scripts run these through `npm --prefix frontend ...`.
